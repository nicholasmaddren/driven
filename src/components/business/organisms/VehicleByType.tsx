import React, { FC } from 'react';
import styled from 'styled-components';

const StyledVehicleByType = styled.div``;

const VehicleByType: FC = () => (
  <StyledVehicleByType>
    <h2>Shop by type</h2>
  </StyledVehicleByType>
);

export default VehicleByType;
